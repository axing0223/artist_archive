# 更新日志

本项目的所有重要变更都记在这里。版本号同时出现在三处（`package.json`、`图片取图扩展/manifest.json`、
`图片取图扩展/安装与测试.md`），改版本时一起改；扩展设置页读的是 `chrome.runtime.getManifest()`，会自己跟着变。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.0.0] — 2026-09-22

从 0.9.0 到 1.0.0 共 33 个提交（2026-09-21 ~ 2026-09-22）。这一版把「整理画师库」的日常动作补齐了：
书钉、画风拟合、卡片上直接刷新，外加一轮筛选与管理的体验修正。

### 新增

- **书钉**：把一张画师卡片钉在列表顶端浮动显示，滚动时一直看得见。钉多张时**一行一张往下排**，
  浮动区内部可滚动，可**收起/展开**，标题上带「书钉 · n / 上限」计数。浮动区里放的是
  **原来那张完整卡片**，那排按钮和作品格全都能用，**在浮动卡片里点「编辑」也会就地展开成编辑器**。
  同时最多钉几张由「设置 → 书钉数量上限」决定（默认 3，范围 1–12）；钉满时其他卡片上的书钉按钮
  立刻置灰并写明原因，取消一个立刻恢复。上限调小到比现有书钉还少时，**已有的书钉不会被踢掉**。
- **画风拟合**：卡片上新增的标记位，标过之后可用「筛选 → 特殊筛选 → 画风拟合」一次挑出来。
  它和「参考评分」是两件事：评分说的是多喜欢，这个说的是"这张参考图能代表画风"。
- **卡片上的「刷新」**：重新读站点上的正式名、笔名、作品数量与截至日期前数量，**读完直接落盘**，
  不必为了刷数据先进编辑态。编辑态里那个同名按钮已删除。
- **卡片按钮重排**：顺序固定为 删除、画师页面、刷新、画风拟合、书钉、编辑。
- **筛选改成三态**：每一组分类/标签都是「不选 → 正选（只看这些）→ 反选（排除这些）→ 不选」。
- **特殊筛选新增**：无作品图、画风拟合。
- **管理分类与标签**：每一行直接显示**有多少位画师在用**这个分类/标签。
- **例图占位**：作品的格数不够时，测试风格图也会占一格，不再出现空着的格子。
- **应用图标**铺到全部位置：页头品牌位、浏览器标签、takoma 漂浮窗、扩展测试页。

### 修复

- **取消书钉后作品不再加载**（用户报的）：钉住再取消，回到列表的那张卡片作品格是空的。
  根因有三层，逐层修掉——作品格的 `<figure>/<img>` 是跨卡片复用节点的，占位被回收时它们已被
  解绑、搬回来时没人重新绑定；占位清空后卡片指纹还是旧的，下一次渲染会把这张卡片判成"没变化"
  直接跳过；而 IntersectionObserver 对「元素离开文档」和「同一批元素再插回文档」都不回调。
  现在复用作品格时按当前卡片的绑定组重新绑一次，占位被清空时留一个"指纹作废"的记号，
  图片加载器也多了 `announce()` 由画廊按实际几何通报可见性。
- **书钉卡片点「编辑」没反应**：浮动区原来写死用浏览态卡片重画，编辑器永远不出现；
  而且浮动区那个画廊实例不知道"这张正在编辑"，会把它判成"没变化"。两处都已修好。
- **书钉编辑时看不到下半截**（用户报的）：编辑期间浮动区取消吸附与限高，整张编辑器都能滚到。
- **反选态失效**：分类/标签的反选以前常态不生效，现在改成实心红；「全部」不再跟着变红。
- **翻页会动视线**：作品选择器翻页不再移动焦点与滚动位置，只在原地换内容。
- **悬停过强**：悬停只作用于指针所在的那一格／那一张卡片，不再整屏联动。
- **删除作品后选择器不同步**：删掉的作品在选择器里仍勾着，再点一下反而又加回来。
- **关闭编辑器后测试图还显示**：关闭时彻底不再显示测试风格图。
- **设置面板打扰编辑**：调设置不再重建正在编辑的输入框或选图器。
- **存盘失败不再报「完成」**；导入按钮上膛时真的变红。
- **图标镜像被文本处理弄坏**：页头图标一度显示不出来，已修复。
- **最小 Chrome 版本**：扩展声明 `minimum_chrome_version: 116`。

### 性能

- **改标签/改分类保存极慢**：给纯元数据改动开了快路径（只重写真正变了的画师），
  并让没变的索引不再重写。实测 1366 ms → 471 ms（`npm run test:edit-performance` 可复现）。
- **批量操作**：新增、采集、测试图导入与备份导出的合并保存与分批落盘
  （`npm run test:bulk-performance` 可复现数字与边界）。
- **写盘并发**：并发池（`WRITE_CONCURRENCY=6`），不再一位画师一条串行等。
- **渲染**：书钉状态不再让所有卡片的指纹一起失效，改成独立同步按钮状态。

### 安全与可靠性

- **来源校验**：`background.js` 全部 7 个消息分支都校验 `sender?.id !== chrome.runtime.id`。
- **待办队列不再整批丢失**：从「先删再返回」改成「领走不删，等页面 ack 之后只删点名的那几条」，
  流程交接协议多了一条 `{channel:'artist-library-page',type:'ack',requestIds:[...]}`。
  页面被关掉或崩掉时，那批建卡动作下次打开还会再来一遍（重复建卡会被「已经在库里」挡住）。

### 文档

- 新增 `README.md`（项目是什么、30 秒上手、功能清单、仓库结构、常用命令）、
  `隐私说明.md`（逐条对着源码写：数据存在哪、会连哪 4 个站点及带什么凭据、卸载后怎样）、
  **首次安装**与**卸载与数据**两节（在 `图片取图扩展/安装与测试.md`）。
- 四份用户文档里已经失效的说法逐条核对改完（`使用说明.md`、`安装与测试.md`、
  `takoma-助手.md`、`竖屏工作台说明.md`），文档里不再写死测试条数。

### 测试

- `npm test` **352 项，连跑 10 次全绿**；另有七个真实浏览器验收脚本
  （`test:browser`、`test:ui`、`test:pin-performance` 与四个性能回归），1.0.0 发布前全部跑过。
- 发布产物**单文件 `画师库.html` 用 `file://` 直开实测通过**：外部 script/样式表各 0 个、
  样式表已生效、关键模块与界面齐全、零页面异常与零 `console.error`
  （`npm test` 只校验它与 `app/` 同步，不验它能不能跑）。
- **没验的三件事（如实记录）**：真实 Danbooru / NovelAI 联网路径（脚本里的站点请求都换成了固定数据）、
  真实硬盘（非 OPFS）吞吐、在 Chrome 里手动加载扩展后的实际观感。

### 内部（发版前复核后补的两处加固）

- `app/image-loader.js` 的 `bind()` 快路径补回「重新观察一次」：相同来源重复绑定时不再跳过
  `unobserve` + `observe`。这一对调用正是"每次重绑都会重新触达一次可见性"的保障，
  去掉之后图能否显示就完全依赖调用方补通报——作品格是跨卡片复用节点的，这条不能少。
  补回不会取消在途加载、也不会丢弃已显示的图（`show()` 对已有 `objectUrl` 的记录是空操作），
  书钉专项与密度回归都确认没有引入多余重排。
- `app/workspace.js` 的书钉定位移到初始化守卫之后：重复 `init()` 不会再叠加一个
  `ResizeObserver` 与一个 `resize` 监听。

---

## [0.9.0] — 2026-09-21

初始版本：单文件 `画师库.html` + Chrome MV3 扩展「画师库 · 图片助手」。
功能包括：本地数据文件夹（File System Access）、画师卡片与作品格、四组分类与标签、
参考评分、测试风格图（导入 / NovelAI 生成 / 批量生成）、Danbooru 采集与作品选择器、
查看器、备份导出与恢复、深浅主题、竖屏布局、takoma 提示词助手。

[1.0.0]: ../../compare/v0.9.0...v1.0.0
[0.9.0]: ../../releases/tag/v0.9.0
