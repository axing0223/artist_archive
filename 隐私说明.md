# 隐私说明

这份说明写的是**代码实际做的事**，逐条都对着源码，不是套话。
如果你要核对，括号里给了文件与行为；改代码时请一并更新本文档。

一句话版本：**画师库不往任何服务器上传你的数据。它没有自己的服务器。**
你的画师资料与图片全部存在你自己选的文件夹里；扩展只在**你触发某个功能时**，
代表你去访问下面列出的那几个站点。

## 数据存在哪里

| 存在哪 | 存了什么 | 谁能看到 |
|---|---|---|
| 你选的「数据」文件夹 | 画师资料（`信息.json`）、缩略图与原图 | 只有你。扩展与网页通过 File System Access 读写，不经过网络 |
| 浏览器 localStorage | 界面偏好：主题、卡片尺寸、布局 | 只有你 |
| 浏览器 IndexedDB | 你上次选的那个文件夹句柄，用来"下次自动接上" | 只有你 |
| 浏览器扩展会话存储 | 画师库页面没开着时的**待办动作**（内存态，关浏览器即消失） | 只有你 |
| 扩展的 `chrome.storage.local` | 提示词助手的浮窗尺寸 | 只有你 |

**没有服务器端。** 没有账号、没有同步、没有遥测、没有崩溃上报。

## 扩展会连哪些站点，带什么凭据

只有下面这些，而且都是**你触发对应功能时**才发起：

| 站点 | 什么时候连 | 带什么 | 用途 |
|---|---|---|---|
| `danbooru.donmai.us` | 你搜索/添加画师、刷新数量、采集作品 | **带你的登录 Cookie**（`credentials:'include'`） | 取画师与作品数据。图片地址字段站点只对"可见用户"返回，带登录态才拿得到 |
| `cdn.donmai.us` | 需要某张图的字节时 | 同上 | 取图片本身 |
| `image.novelai.net` | 你在画师库/测试页点「生成」时 | **`Authorization: Bearer <你的 token>`**，且 `credentials:'omit'`（不带 Cookie） | POST `/ai/generate-image` 文生图 |
| `api.novelai.net` | 需要显示剩余额度时 | 同上 | GET `/user/subscription` 查套餐与点数 |
| `file:///*` | 页面加载时 | 不发请求 | **只读 DOM**，用来在本地网页与扩展之间传消息 |

几点值得说清楚的：

- **Danbooru 的请求会带上你的登录 Cookie**（`probe.mjs` 的 `request()` 默认 `credentials:'include'`；
  `app/host-direct.js` 直连那条路同样是 `include`）。这不是"偷偷带"——必须带，否则拿不到图片地址。
  这些请求只发给 Danbooru 自己，不会转发到别处。
- **NovelAI 的 token 只进请求头，不落盘、不写日志**。它由页面交给你自己填的那份配置，
  扩展只负责放进 `Authorization` 头（`probe.mjs` 的 `bearerToken`）。查询额度的请求**不带** Cookie。
- **地址不是任意代理**：生图通道把地址**写死**成 `https://image.novelai.net/ai/generate-image`，
  额度通道只认 `/user/subscription`（`probe.mjs` 的 `novelaiUrl`、`subscriptionUrl`）。
  画师库页面无法让扩展去请求别的地址。
- **`host_permissions` 只声明了 4 个域名**（见 `manifest.json`）：
  `danbooru.donmai.us`、`cdn.donmai.us`、`image.novelai.net`、`api.novelai.net`。
- **扩展不申请任何全站访问权限**。右键菜单那条路用的是 `activeTab`：只有**你主动在那一页用菜单**时，
  才临时获得那一页的注入权限，用来画一朵结果提示。

## 内容脚本（网页里跑的脚本）

两个内容脚本，行为都很有限：

- **`content.js`**：只在**顶层 `file://` 页面**且该页带 `<meta name="artist-library" content="v1">`
  时启用（`content.js` 第 2 行的判断）。它**自己不联网**，只是把本地网页的请求转给扩展——
  因为 `file://` 页面受跨站限制，必须由扩展代发。非顶层框架、非 file:// 协议、没有那个 meta 标记的页面，
  它直接不启动。
- **`takoma.js`**（提示词助手，实验性）：只在 `staging.takoma.app` 注入（manifest 里限定了域名）。
  它**不读取页面的输入框内容之外的东西，也不发任何网络请求**——查库、查 Danbooru 都是通过
  `chrome.runtime.sendMessage` 请扩展去做。它读的是你双击的那段提示词文字（用 DOM 结构切出完整标签），
  以及浮窗自己的尺寸。

## 它会读取你在网页上选中的文字吗

会，但**只在你主动触发时**：

- 你在任意网页上**选中文字 → 右键 → 「添加到画师库」**时，扩展读取那段选中的文字
  （最多 200 字符），用它去 Danbooru 查这位画师并建卡。
- 你在提示词界面上**双击一个提示词**时，读取那一段文字。

除此之外不读取任何页面内容。选中的文字**不会**被存进浏览器之外的地方；它要么变成你库里的一张画师卡片，
要么在失败时随错误提示消失。

## 你的数据不会被上传

- 「导出备份」是**写到你本机**的一个 JSON 文件（只有资料与图片地址，不含图片字节）。
- 「导入备份」是**从你本机**读一个 JSON 文件。
- 图片只在两种情况下离开你的机器：你主动点开某张作品去看原图（从 `cdn.donmai.us` 取回），
  以及你主动点「生成」时把提示词发给 NovelAI。**没有反向的上传通道。**

## 卸载会怎样

**先备份，再卸载。** 卸载扩展**不会**删除你的数据文件夹——数据始终在那个文件夹里，
和浏览器、和这个扩展都没有绑死。

1. 想留档：直接**复制整个「数据」文件夹**（JSON 与图片一起）。这是唯一完整的备份方式。
2. `chrome://extensions` → 找到「画师库 · 图片助手」→ **移除**。
   这会一并删掉扩展的 `chrome.storage`、会话存储与 IndexedDB 记录（也就是"上次用的文件夹"这个记忆）。
3. 你的「数据」文件夹**原样留在磁盘上**。以后重新装回扩展、或直接双击 `画师库.html`，
   重新选一次那个文件夹就接着用。
4. 想彻底清除：手动删掉那个「数据」文件夹，以及浏览器里扩展留下的记录（移除扩展时已清）。

> 注意：**浏览器自身的清除浏览数据**（清除 Cookie/站点数据）会清掉 Danbooru 的登录态与
> 扩展的 IndexedDB 记忆，但**不会**动你的「数据」文件夹。

## 更新这份说明

改了任何网络行为、权限、存储位置，就必须同步改这里。
`manifest.json` 的 `host_permissions`、`permissions`、`content_scripts.matches`
是这份说明的"事实来源"，核对时从它开始。
