# 竖屏作品对比工作台

分支：`codex/artist-library-portrait`。从稳定提交 `2dfabad` 创建，未合并至 main。上一版界面草案保留在 `codex/artist-library-ui-upgrade`（`eef98f7`），不属于本次实现。

## 页面与交互

- 以 1080×1920 竖屏电脑为主要布局：名称、分类和标签合并在同一行，参考评分以数字盾形徽章放在卡片左上角，1–5 分分别用灰、绿、蓝、紫、金色底板，名称与标签之间用竖线分隔；五张图片完整同排，补充资料在下。作品预览默认高度 190px，设置中可在 140–360px 逐像素调节并即时生效；两种密度均使用设定的实际高度，紧凑视图压缩卡片留白，保留已放大的字号。所有窗口尺寸都保留五列；图片使用 contain，不裁切，不让用户横向滚动找图片。
- 顶部搜索与分类保持可用；画风标签、评分收纳进筛选面板。搜索覆盖名字、笔名、别名、标签、备注和序号，支持多个词同时匹配；新增名称、评分、图片数量排序。
- 深色和浅色主题、舒适与紧凑视图保存在本机。新增可移除筛选条件、明确的空状态、活动记录、快捷操作、图片前后导航，以及空图片位置的文件选择入口。
- `/` 聚焦搜索，`Ctrl/⌘ K` 打开快捷操作，编辑时 `Ctrl/⌘ S` 保存，预览时 A/D 或左右方向键切换图片，`Esc` 关闭浮层。尊重系统减少动态效果设置，包括设置在页面打开后发生改变的情况。
- 卡片操作依次为删除、画师页面、编辑；删除需二次点击确认。回到顶部常驻底部状态栏。作品详情用独立的图片区域完整适配原图比例，单行说明不产生多余滚动条，长说明按需滚动。
- 所有弹窗的打开和关闭均有过渡，覆盖按钮、Esc 与遮罩；筛选面板、备注与操作菜单补齐展开收起过渡。退场完成后才释放原图，快速重开不会被旧回调清空。
- Danbooru 作品选择区每页三行七列，排序、当前页与上一页/下一页集中在顶部。移除预览大小、批量勾选和加载更多控件；A/D 或方向键在作品区翻页，表单输入与图片弹窗不会误触发翻页。勾选跨页保留，翻页释放上一页图片绑定，排序请求过期后不覆盖新页面；末页未满时保留空位，读取失败可重试。页码按可添加作品计算，总页数在读到末页后显示。
- 手动展开聚焦候选作品并校准滚动位置，自动展开保留编辑位置。编辑按钮在右上角依次为删除画师、刷新、取消、保存。切换分类聚焦第一张画师卡片；站点作品数紧随名字并用竖线分隔，预览切图按卡片最终显示顺序。所有删除二次确认文字为白色。
- 顶部 NovelAI 额度分别显示估算张数、剩余百分比和 Anlas 点数，点击刷新；并发点击合并查询。演示页面的作品分页与额度均为虚构数据。
- 编辑草稿使用独立生命周期，筛选、排序和后台进度更新不会重建输入框与选图器。危险操作保留二次确认。

## 模块与性能

`app/library-index.js` 管理资料快照索引、统计、组合过滤和稳定排序，`app/workspace.js` 管理页面导航、主题、焦点和会话活动。`app/app.js` 继续协调资料写入及采集/生成任务；数据文件格式和已有迁移机制保持兼容。

虚拟列表仅测量附近节点，内容变化前释放旧图片绑定，复用现有卡片和观察器；密度或宽度变化修正占位估计。图片继续使用并发限制及有界缓存。两万条资料的索引测试确保连续搜索不重复扫描作品或重建全文索引。

## 预览与构建

在本分支目录执行：

```powershell
npm run preview
```

打开 http://127.0.0.1:4175/app/index.html?demo=1 。预览采用虚构画师和程序绘制的演示图片，存放于当前浏览器的独立 OPFS 目录；不连接电脑上的真实资料库，不调用实际采集或付费生成服务。演示脚本位于 tools，不进入正式单文件及扩展构建。

`npm run build` 只更新当前分支目录下的 `画师库.html` 与扩展镜像。不要用此工作目录以外的原项目目录运行，除非准备合并和更新当前扩展。

## 验证

- `npm test`：业务、文件存储、查询索引、资源管理、图片预览和构建一致性回归。
- `npm run test:browser`：在独立浏览器配置中检查真实文件保存、后台刷新合并、图片拖放替换与备注编辑。
- `npm run test:ui`：1080×1920 及 390px 窄窗口的五图同行/contain、主题、表单名称、键盘操作、中文输入、草稿保留、2000 位画师长列表及减少动态效果；截图与测量报告写入忽略的 `.ui-artifacts/`。

真实浏览器测试需要 Node.js 22+ 与 Chrome 或 Edge，必要时通过 `BROWSER_PATH` 指定浏览器。性能报告的列表数据为演示快照，不代表真实网络或磁盘加载耗时。实际站点采集与付费生图未在此次验收中执行。
