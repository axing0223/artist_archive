# 图片助手 0.3.2：取缩略图与原始大图

之前的单图测试已通过；现在扩展可以给本地「画师库.html」提供图片，并支持按作品编号问出原图地址。测试页面仍保留，点击扩展图标可以打开。

## 更新步骤

1. 打开 Chrome 的 `chrome://extensions`，或 Edge 的 `edge://extensions`。
2. 找到原来安装的扩展，点击「重新加载/刷新」。更新后名称为「画师库 · 图片助手」，版本为 **0.3.2**。不需要重复安装第二个扩展。
3. 打开扩展的「详情」，开启 **允许访问文件网址**。这是为了让连接脚本在本地 HTML 中运行；如果不开，原单图测试可能成功，但画师库仍无法连接。
4. 关闭旧画师库标签页，重新双击项目根目录的 **画师库.html**。
5. 顶栏应显示 **图片助手已连接 · 0.3.2**，再选择原来的数据文件夹。若显示未连接，检查上一步的开关，点击状态按钮重试。

0.3.2 起，画师的作品列表、画师检索与数量查询改由扩展代为请求，不再由网页匿名直连。原因是站点对 `file_url`、`large_file_url`、`preview_file_url` 标注了 "Only shown if visible to user"——匿名请求拿不到这些图片地址，带登录 Cookie 才拿得到。**更新到 0.3.2 必须重新加载扩展**，否则这条通道不存在，网页会自动退回匿名直连（能取到数量，但取不到图片地址）。

0.3.1 起，接入扩展的判定改为页面自身的 `<meta name="artist-library" content="v1">` 标记，不再要求文件必须叫「画师库.html」。这样 `工具/回填作品.html` 这类本地工具页也能取图。

0.3.0 换了取图协议：扩展与网页必须同版本，旧版扩展配新版网页会在点开作品时报「扩展返回的图片尺寸信息无效」。重新加载扩展即可。

## 正式使用

- 识别候选画师时，会按页取回最近 20 张作品，每张都带勾选框；勾选的作品在添加时下载缩略图。
- 主页面与候选区只加载缩略图（360×360）。点开作品时才临时取回原图放大显示，不再写入磁盘；单张上限 50 MB，关掉页面即从内存释放。
- 旧版本加进来的画师没有保存原图地址，点开时会先按作品编号向站点查询再取图；查不到就继续显示缩略图并说明原因。
- 添加画师时，会尝试把勾选作品的缩略图保存进独立画师目录。此后优先读取本地文件。
- 已有画师中以前加载失败的在线图片，可以直接通过扩展查看；点击 **缓存附近预览**，将点击时附近已加载画师的在线缩略图保存到本地。不扫描或下载全部 2000 人。
- 所有画师在一整页里连续上下滚动，没有分页。离视野较远的卡片只保留占位高度，靠近时再构建卡片和加载图片。
- 点击 **释放临时缓存** 会释放内存中的图片并按需重载当前可见图片，不删除磁盘文件。

## 大量画师的加载与缓存

- 启动只并发读取少量「信息.json」，不把整个库的图片转成 Base64。
- 文件夹里的图片保留为路径；进入视野附近才读文件。缓存未命中时，旧在线图片才通过扩展请求。
- 最多同时加载 3 张图片；已离开视野的排队任务会取消，显示用的 Blob URL 会撤销。
- 临时 Blob 缓存最多 64 张且不超过 32 MB，最久未使用的条目优先淘汰。这不是整个浏览器进程的总内存上限；解码后的可见图片、资料、浏览器自身也会占内存。
- 已保存的本地图片是你的持久资料，不因内存缓存淘汰而删除。再次打开、滚动回来时可从本地读取。
- 卡片展示前 5 张预览；上传超过 5 张的画师可在编辑页查看全部，图片仍按需加载。
- 数量、备注等修改只写变化的画师资料，不重复读取和写入所有图片。
- 大库优先复制整个「数据」文件夹备份。JSON 导出逐张读图写入，避免同时组装整个图像库；JSON 导入限制为 100 MB，更大备份请使用完整数据文件夹。

## 权限与限制

扩展联网权限仍仅限 `danbooru.donmai.us` 和 `cdn.donmai.us`。增加了文件页面连接脚本，脚本只在带有画师库标识（`meta[name="artist-library"]`）的本地顶层页面启用；后台再次校验来源必须是本扩展注入的、顶层框架的 `file:` 页面。0.3.2 增加了一条只读的接口通道：网页可以请求 `danbooru.donmai.us` 上以 `.json` 结尾的地址，用于取画师与作品数据；非该域名、非 https、带账号密码或自定义端口的地址一律拒绝，接口返回也限制在 8 MB 内。不提供任意本地文件读取，不读取或导出 Cookie，不改变浏览器安全设置。

单次请求最多等待约 60 秒，单张图片上限 50 MB。图片以分块方式传回网页（每块 4 MB），避免大图撑爆一次消息；分块中途扩展被回收时会提示重新获取，不会返回残缺图片。服务器仍可能拒绝某些请求，失败会提示并保留原来源链接，30 秒内避免反复自动请求同一失败图片。

本轮代码测试包括模拟的 2000 位画师/10000 张图片、内存缓存淘汰、连续滚动挂载释放、连接消息往返、权限边界、取消和备份。实际 Chrome 文件页面连接仍需按以上步骤更新后确认；模拟耗时不是实际磁盘或网络性能承诺。
