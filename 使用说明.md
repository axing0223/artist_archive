# 画师库 · HTML 版

有**两种打开方式，功能和数据完全一样**，随你顺手：

- **双击「画师库.html」**：不需要装扩展的壳，取图与生图由「画师库 · 图片助手」扩展代发（要开「允许访问文件网址」）。
- **点扩展图标**（0.5.0 起）：在扩展自己的页面里打开画师库，站点请求由页面直连，不用再绕一层、也不用开那个开关。

两种方式读写的都是你选的那个「数据」文件夹，随便换着用，数据不会分家。区别只在于「谁来发请求」：`file://` 页面受跨站限制，必须让扩展后台代发；扩展页本身就是扩展，带登录态直连即可。

双击「画师库.html」，使用 Chrome 或 Edge 打开，不需要 CMD、Node.js 或本地服务。

1. 点击「选择数据文件夹」。
2. 选择项目里原有的「数据」文件夹，并允许网页读写。也可选择一个空文件夹建立新库。
3. 正常添加、分类、编辑画师，保存会写入所选文件夹。

每次重新打开网页，**会自动接上你上次用的数据文件夹**：同一浏览器会话里（刷新、重开标签页）通常直接可用，不用再点；浏览器完全重启后，浏览器会要求你点一下确认——页面上会出现一个「继续使用上次的文件夹「数据」」按钮，点它即可，比重新走一遍文件夹选择框省事。万一那个按钮没出现，或者你想换一个文件夹，点顶部「数据库文件夹」照旧。

记住的是「上次选的那个文件夹」，不是把数据搬进浏览器：**数据始终在你的文件夹里**，换电脑、换浏览器、换壳（`file://` 与扩展页）都只是各自记各的。注意两点：`file://` 打开的网页与扩展页是**两个不同的源，各记各的**；如果浏览器里禁用了 IndexedDB（少数隐私设置会这样），就记不住，页面会明确告诉你「这个环境记不住文件夹，下次还得重新选」，功能本身不受影响。选择文件夹前编辑功能暂不可用，避免误把空库覆盖到已有数据。不支持文件夹访问的浏览器会明确提示使用 Chrome 或 Edge。

数据格式：

```text
数据/
  画师库.json
  画师/
    0001-画师tag-画师编号（手动画师写作 0001-名字-manual）/
      信息.json
      缩略图/
      大图/（只有你自己上传的图片会写到这里）
```

图片文件名有两种：普通作品图与上传的图片按内容哈希命名（24 位十六进制，同一张图不会重复保存）；**测试风格图按「画师tag-测试风格N」命名**，方便在资源管理器里直接认出是哪位画师的哪一格，缩略图与原图同名、各自带自己的扩展名。清理旧图片时只删这两种自己写出来的名字，你手动放进「缩略图 / 大图」的其它文件不会被碰。

目录名由「四位序号-画师tag-编号」组成。序号在添加时发放，之后不再变化，删除画师空出的号也不回收，因此目录名不会因为排序变化而被改名。打开旧版本（目录名为 danbooru-编号）的数据时，会在你第一次保存时自动升级目录名与目录结构，无需手动迁移；升级前建议先复制一份「数据」文件夹。**要留住图片本身（原图、上传的图、生成的测试风格图），只能复制整个「数据」文件夹**；网页里的「导出备份」只导出资料与图片地址，不含任何图片数据（见下方「备份包含什么」）。

底部输入画师标签、别名、Danbooru 编号或主页 URL，会显示匹配候选、作品数量与最近 20 张作品。这里的勾选框只表示「这次要保存哪些」，默认都不勾选。点作品下方的编号可在页面内放大查看原尺寸大图，不跳转站点，原始页面链接在放大视图里。点「添加此画师」才下载勾选作品的缩略图（360×360）；原图不保存，只有你点开作品放大时才临时取回显示，关掉即释放。联网查询受站点访问和跨站限制影响，失败时仍可手动输入并上传图片。

候选区没有「全选 / 全不选」和「加载更多」这两个按钮：勾选就是一位一位点，列表固定读最近 20 张。（「全选 / 全不选」那个名字属于「删除测试风格图片」的对话框，和这里不是一回事。）想批量调整已入库的作品，用画师卡片上的「编辑」。

**在别的网页上看到画师名字，不必复制粘贴**：选中那段文字（标签、别名、编号或主页链接都行），右键 → 「添加到画师库」，它会**直接建一张新画师卡片**（等价于「批量采集画师」加一位：补 Danbooru 编号、作品总数、3 张作品缩略图），你正在看的那一页不会被切走，**也不会自己打开画师库页面**。

浏览器右上角会飘出一个提示：先是**琥珀色的「正在尝试加入画师库…」**（带个闪动的小圆点，一直在），建完就地变成——

- **绿色**：已添加「XX」· Danbooru #编号 · N 张作品。
- **红色**：没能添加，并写明原因（站点上没找到 / 匹配到多个候选需要手动确认 / 已经在画师库里 / 没有可用的数据文件夹 / 取不到作品）。
- **蓝色「已记下这次添加」**：画师库当前没开着。这时**不会自动开页面**，等你下次打开画师库时会自动把这张卡建好（打开时状态栏会说明）。

**点那个提示**就会打开（或切到）画师库，并自动滚到刚建的那张卡片、闪一下边框。定位不到唯一的画师、或一条作品都取不到时**不会建卡**——宁可让你手动来一次，也不留一张认错人的空卡；这时提示是红色的，点它只会把画师库打开，**不会**替你填任何输入框（要手动加就点页面上的「添加画师」，自己把名字填进去）。

这个菜单由「画师库 · 图片助手」扩展提供，而且**只投递给扩展页里的画师库**（双击 HTML 打开的那份收不到）。真正的写文件必须由那个页面来做（数据在你选的文件夹里，只有页面拿得到那套文件夹读写逻辑）。

序号右侧显示「作品数量：最新数量（截至日期数量）」，旁边的「刷新」只更新当前画师的数量，不刷新图片。读取时间保存在数据中，但不显示在页面。「数据截至日期」在右上角的「设置」里统一修改，默认 2026-07-01；修改后保留已有数量，直到点击对应画师的刷新。悬停数量可查看该画师旧数量实际使用的截至日期。候选识别区只显示最新数量；添加时按设置里的截至日期读取数量。日期统计的是该日期之前上传、当前仍可查询的作品，不是历史数据库快照。

「管理标签」里可以同时管理主分类和画面感觉标签，都能新增、重命名、删除。重命名分类会同步更新已分类的画师；删除分类后，这些画师回到「待判断」，画师和图片都不会丢。标签的规则相同。主分类列表保存在「画师库.json」里，可随数据文件夹一起迁移。

**每一行右侧的数字**是「有多少位画师直接在用这个分类 / 这个标签」，跟顶部筛选按钮上的数字是同一个口径：分类只看直接属于它的画师数（一位画师只属于一个分类，所以各分类相加不会超过总人数）；标签只看直接带它的画师数。**显示 0 的仍然留在列表里**——那正是没人用的分类或标签，可以据此决定要不要删掉，不会因为它没被使用就自己消失。数字只统计当前数据文件夹里读到的画师。

「导入测试风格图」可以一次选多张图片，并填写两个序号：**起始画师序号**决定从哪位画师开始分配，**测试风格序号**决定这些图片记为几号（默认 1）。导入的图片都记为指定序号，某位画师已经有该序号时会自动顺延到下一个空位。选图过程中会按画师逐个列出「文件名 → 序号」的分配预览，图片多过画师时多余的会跳过并提示。导入会分批处理并保存，状态栏显示已保存张数；后面的图片解码失败时，前面已保存的批次保留，修正剩余图片后可以继续。导入期间收起对话框不会中断处理，也不能切换资料库。

测试风格图片和作品图存在一起，但显示规则不同：卡片预览固定 5 个格子，测试风格图片从最右边往左按序号排（序号 1 在最右），作品从左往右填剩下的格子，中间空出的格子留白。所以某位画师只有 2 张作品时，中间会空着，测试风格图片依然待在最右边。

设置里的「固定测试风格图」会把最右 2 格钉给「测试风格 1」「测试风格 2」：作品图只排左边 3 格，测试图再多也挤不掉作品，作品再多也占不到这两格。这两格空着时，里面会出现「生成」按钮：**第一次点击只是把按钮点亮（并且写出代价），再点一次才真的发出去**，不再弹系统对话框。等待期间格子里显示转动的环和进度文字，返回后图片直接回填到对应格子，并按「画师tag-测试风格N」命名存进画师目录（原图是 .png，缩略图同名换成 .jpeg），生成时用的提示词记在这张图的说明里，方便以后复现。开着这个开关时，多出来的测试风格图（序号 3）排到左边，序号 4 以后不再显示在卡片上——数据还在，关掉开关或用「批量删除测试风格图片」都能看到它们。

**一次点好几格也没关系**：需求会排队，一次只跑一条，两条之间隔 **5 ± 3 秒**（也就是 2–8 秒之间随机）再发下一条，免得一口气打过去被站点限流。第一格立刻开始；排在后面的格子会显示「排队中 N」，状态栏会写「已排入队列：第 N 位」。同一格重复点不会排两次。一条失败不会卡住后面的——失败原因写在状态栏，队列继续往下跑。还没跑完就关页面，浏览器会先问一下（排队的图会丢）。

同一个对话框下半部分是「批量删除测试风格图片」：按**序号**勾选（每个序号会显示涉及几位画师、共几张），删除时移除所有画师中该序号的测试风格图片，作品图不受影响。

卡片上的每一格都能直接用鼠标操作：

- **把电脑里的图片拖进某一格**，就会快速放进那一格（自动生成缩略图并存下原图）。拖到哪一格就换哪一格：那一格是空位就排在那里，已经有图就把这一格的图换成你拖进来的（原来那张会被替换掉，包括它的 Danbooru 编号与链接）；是测试风格格就按那一格的序号存成测试风格图。一次拖多张时从这一格往右依次填，左边 3 格填满后会继续落到两个测试风格格上，5 格都用完则追加到作品列表末尾（卡片上不一定看得到，编辑页里能看到）。拖进来的图片必须先是**你已经保存过的格式**：JPG、PNG、WebP、GIF、AVIF，单张不超过 50 MB。
- **左下角的 `#编号` 可以直接点**，会在新标签页打开对应的 Danbooru 作品页；测试风格图没有编号，只有「测试风格 N」几个字，点不了。作品没有编号但有来源链接时，那里会显示「来源 ↗」。
- **右下角是「删除」**：第一次点变成「再点一次删除」，再点一下才真的把这一格删掉（图片文件也会一起清掉）；不点它、4 秒后自己恢复。删除只影响这一格，其他格子和画师资料都不动。

把图片拖到格子以外的地方不会发生任何事——整页都不接受文件拖放，免得浏览器直接打开那个文件、把没保存的改动冲掉。

点画师卡片上的「编辑」，卡片会**就地变成可修改状态**：名字、主分类、标签、页面链接、画风描述、备注直接改成输入框，改完点「保存」写入数据文件夹，点「取消」放弃；标签用顿号或逗号分隔。顶部那行「作品数量：N（n2）」里，括号外是**这本库里存了几张**，括号内是**站点上这位画师一共有多少张**（点「刷新」读取；想一进编辑态就自动读，见设置里的「编辑画师时自动展开 danbooru 作品」）。编辑中的卡片不会被滚动卸载，可以放心上下翻着改，卡片里的作品也能逐张「移除」。**「删除画师」就在卡片右下角「编辑」的旁边**，和编辑态里那个删除按钮是同一套逻辑：点第一次变成「再次点击确认删除」，4 秒内再点一次才真的删掉，超时自动复原。保存、改设置、搜索、跑检测这些操作**只重画真的变了的那张卡片**，不会再让整屏卡片一起闪一下；删除画师时剩下的卡片是**滑上来**的，加载新作品时新格子各自淡入。

卡片下方是「从 Danbooru 添加作品」：点标题旁的「展开读取」（展开后同一个按钮写「收起」）才按画师名字查询站点作品。**勾上某一张就等于把它加进这本画师的作品列表**——立刻下载它的缩略图并补进上面的作品格，再点一次取消勾选就移出。列表用「← 上一页 / 下一页 →」翻页，每张都要自己勾，**没有「全选 / 全不选」**。库里已有的作品不会重复列出，加过的候选右下角会留一枚「已加入」。这些改动仍然只是**草稿**，点「保存」才写进画师目录。换「采集作品的排序」时会先把旧列表留在原地、等新的读回来再整体换掉，换完视线回到画师的作品格上，已经加进去的那些照旧打着勾。想加本地图片，把文件**拖到某一格上**即可（会同时生成缩略图并保留原图）。删除画师会清理其独立目录，移除图片并保存后会清理对应图片。

「app」是网页源代码，「build.mjs」仅供开发时重新生成单文件网页。日常使用只需「画师库.html」和所选数据文件夹。原始「画师表.txt」仍可用于批量采集画师。

「批量采集画师」里有两个开关。「同时采集 3 张作品与数量」是原有的；**「同时生成测试风格图」（默认关闭）** 会在导入完成时给每位新画师排一条「生成测试风格 1」的需求，走的是和卡片上「生成」按钮**同一条队列**（一条一条跑，两条之间隔 5±3 秒）。它**不会拖慢导入本身**——导入先照常跑完、对话框照关，生成在后台慢慢排。每条都会消耗额度或点数，所以**人数超过 20 位时会先让按钮变红**并写明「将导入 N 位、排入 N 条生成需求」，再点一次按钮才真的开始（8 秒不点自动复原）。中途点「取消」中止导入的话，不会排入任何生成需求。**采集按小批次保存和刷新**：名单加进去之后，快速返回的结果会合并保存，通常每 10 位一批；图片较多或处理较慢时会提前保存。数量与缩略图随已保存批次补上，不必等待整个名单结束；同时会自动清空标签/评分/搜索筛选并把分类切到**「待判断」**，新采回来的画师就在眼前。

**采集过程中可以把对话框收起来**：点右上角「收起 ×」（或按 Esc）只是藏起界面，**采集会在后台继续**，进度同时写在页面底部那一行。想停下来要点 **「停止采集」**——工具栏上（对话框收起时）和对话框里各有一个；它会等当前这一位采完就停，**已经采集到的全部保留**，再次提交同一名单会跳过已经完成采集的画师，继续补齐尚未采集或上次失败的画师，不会重复建卡。还没采完就关掉页面时浏览器会先问一下。建议先点「停止采集」，等待尾批保存后再关闭；强行关闭时，当前尚未提交的小批次需要重新采集。工具栏上那个按钮叫**「批量采集画师」**，对话框标题同名。

排了长队要反悔：顶部会多出一个 **「排队 N · 清空」**（只在真的有人排队时出现），点一下就取消所有还没开始的需求——正在跑的那条会跑完，不浪费已经发出去的请求。

开发时在项目目录运行 `npm run build` 重新生成「画师库.html」，运行 `npm test` 执行全部自动化测试（需要 Node.js 20 以上、无需安装依赖）。构建会同时把 `app/` 镜像进「图片取图扩展/app/」——扩展页不能用内联脚本，所以扩展里放的是多文件那一份；镜像由构建生成、不进 Git，测试会校验它与源码逐字节一致。改动「app」里的任何文件后都要重新构建，测试会校验单文件网页与源代码是否一致。Git 里只记录**代码与文档**：画师资料所在的「数据」文件夹整个都被 `.gitignore` 排除，**不要拿 Git 当备份**。你的资料（画师库.json、缩略图、大图）只存在你选的那个数据文件夹里，请直接用文件管理器复制整个文件夹来备份。「数据」文件夹早期被放在了项目目录内，这只是历史原因——它放在哪里都行，换位置后在页面里重新「连接数据文件夹」选一次即可。

构建会覆盖「画师库.html」，请先关闭浏览器里打开的它。除了敲命令，也可以直接双击项目根目录的「重新生成网页.bat」：它会重新生成网页、自动校验产物与源码是否一致，并把结果留在窗口里。若构建提示 Access denied 或 EPERM，说明当前进程没有该文件的写入权限，换到你自己的终端（而不是 AI 工具的沙箱环境）里运行即可。


## 图片助手与大量画师

扩展正式连接步骤见「图片取图扩展/安装与测试.md」。更新到 0.4.1 后，开启扩展的「允许访问文件网址」，重新打开「画师库.html」，确认顶部显示「图片助手已连接 · 0.4.1」。旧资料无需重新导入，但必须重新加载扩展：0.3.0 换了取图协议，0.3.1 改了页面接入判定，0.3.2 让作品列表改由扩展代取（带登录态才拿得到图片地址），0.4.0 增加了代画师库向 NovelAI 请求文生图的通道，0.4.1 又加上了查询 Opus 额度与点数（新增 api.novelai.net 权限）。不重新加载就会一直提示需要更新，也无法生图。

## NovelAI 生图

顶部「设置」右边的 **「生图参数」** 是独立的一页，里面的参数只用来生成测试风格图，全部保存在本机浏览器（localStorage）里：**不写进画师库数据文件，导出备份也不会带上 token**。token 在 novelai.net 登录后从 Account → Get Persistent API Token 取得，形如 `pst-…`；请求由图片助手扩展代发，页面本身不直连 NovelAI。要注意 localStorage 是「这台电脑 + 这个浏览器配置」级的共享存储，本机其他本地网页理论上也读得到，所以换电脑或借人用浏览器时请先把 token 清掉。

参数包括：模型（默认 V5 Full）、尺寸档位（默认竖图 832×1216，另有宽图、方图、大图与壁纸档）、可自己填的宽高、采样步数、Prompt Guidance（引导强度）、**Prompt Guidance Rescale**（0–1，默认 0，用来压高引导带来的过曝发灰）、采样器、随机种子、UC 预设、**质量标签**、**transparent BG**（V5 专有的原生透明背景 PNG）、反向提示词，以及两份提示词模板。反向提示词与两份模板是三个一样宽的大输入框，整行右侧都占满，长提示词写起来不憋屈。

两份提示词模板分别对应最右两格：模板 1 给「测试风格 1」，模板 2 给「测试风格 2」。模板里用 `{tag}` 代表画师 tag（想加前缀就写 `artist:{tag}`），写错的 `{变量}` 会原样保留，方便发现拼写问题。两个模板配同一个画师 tag、换不同构图，比风格更方便。自己填了宽高就以你填的为准，会向 64 对齐、单边限制在 64–2048；NovelAI 还限制总像素不超过 1536 × 2048，超了会在发送前提示你调小。随机种子留空就是每次随机，填数字可复现。反向提示词与两份模板都给了大输入框，长提示词写起来不憋屈。

质量标签与 UC 预设文字都按模型分开处理：V3、V4、V4.5、V5 各有一份。**质量标签**默认开启（跟站点一致），会自动接在提示词后面；不想让它加就在「生图参数」里关掉，关掉后连质量预设的提示字段一起撤掉，服务器也不会再补一份，提示词就是你写的那些。**UC 预设**只提供 Heavy 与「无」两个——其余站点预设的文字没有可靠的公开来源，宁可不给也不猜。这两段文字都由网页自己拼进 prompt 与反向提示词（`qualityToggle` 置 false），不依赖服务器猜。transparent BG 只有 V5 支持，选了别的模型会在发送前拦下来。

### 额度与「是否使用点数」

顶部菜单里的 **额度** 按钮显示 NovelAI 的剩余额度，点一下重新查询；「生图参数」里也有一份更详细的（套餐、免费额度剩余张数与百分比、Anlas 点数、每天回复速度）。查询走的是站点的 `/user/subscription`：NovelAI 把 Opus 的免费额度给成一个会随时间回复的百分比，网页上那个「约 N 张」是按站点客户端的算式 `17.3 × 百分比` 估出来的，**是估算值**，以站点自己的界面为准。

**「是否使用点数」开关决定要不要花 Anlas**：

- **关闭（默认）**：只在 Opus 免费额度内生成——步数会被压到 28 以内、总像素压到 1024 × 1024 以内，一点 Anlas 都不花。这时会先确认免费额度查得到且还有剩：**读不到剩余额度就不允许生成**，免得在不知情的情况下花掉 Anlas。额度用完也照样不允许，提示你等额度回复或打开这个开关。
- **开启**：按你填的参数原样发出，超出免费范围的部分会消耗 Anlas（点数）；点数已经见底又确实超出免费范围时会先拦住并说明。每次生成后会重新读一次额度，如果点数变少了，底部状态栏会告诉你这次花了多少。

### 批量生成测试风格图

工具栏上「导入测试风格图」右边是 **「批量生成测试风格图」**，用来给一批画师排队生成测试图：

- **画师序号段**：填卡片左上角那四位序号（也是画师目录名的前缀）的起止，只给落在这个区间里的画师排队。打开时**默认就填成你当前筛选出来的那一段**——采集完自动切到「待判断」之后，通常直接点开就能用。
- **测试风格序号**：写 `1`、`1,2` 或 `1-2`，最多 20 个。用的是「生图参数」里那两份提示词模板（模板 1 给测试风格 1，模板 2 给测试风格 2）。
- **跳过已经有这个序号的画师**（默认开）：已经有该序号测试图的画师不再重复排队。重复生成不会覆盖旧图，而是把新图存成下一个序号，等于白花一次额度。
- 对话框里会实时算出 **「将排入 N 条」**，并给一句额度上限参考（免费额度模式看 Opus 剩余张数，真要花点数时看 Anlas）。

点「排入生成队列」后，这些需求进的是**和卡片上「生成」按钮同一条队列**：一条一条跑，两条之间隔 5±3 秒，顶部会出现「排队 N · 清空」，点一下能一次取消还没开始的。**条数超过当前额度的上限时会先把按钮变红**、写明超出多少、要求你再点一次才真的排；额度还没查过（读不到）时同样要二次确认；改了范围或序号会立刻撤销这个确认状态。生成回来的图会直接回填到对应格子并写进画师目录。

画师列表采用一整页连续滚动，不分页；只构建附近卡片，图片进入视野附近才加载，远离后释放显示资源。启动只读取资料和本地图片路径。图片加载并发最多 3 个，临时 Blob 缓存上限 64 张 / 32 MB；这个上限不包含浏览器自身和可见图片的解码内存。

右上角的「设置」集中放着：数据截至日期、采集作品的排序、刷新所有画师数据、保存大图、**舒适视图预览高度**、**紧凑视图预览高度**、固定测试风格图，以及「编辑画师时自动展开 danbooru 作品」，每项都带作用说明。开启「保存大图」后，点开作品预览会把该作品的原图保存进画师目录的「大图」文件夹，之后预览直接读本地、也能离线查看；关闭时原图只在本次预览中临时加载，关掉即释放。两个「预览高度」滑块分别调整舒适视图与紧凑视图里每位画师作品预览图的高度（每行显示几位画师会跟着自动变化），设置会被记住。「编辑画师时自动展开 danbooru 作品」（默认关闭）打开后，一进编辑态就自动按画师名字读取站点作品，省一次点击；名字还空着的画师不会自动读。

识别画师的作品列表、以及画师卡片编辑状态下的作品列表，缩略图尺寸由右上角「设置」里的**舒适视图 / 紧凑视图预览高度**两个滑块控制（两处共用同一套设置，改完全局生效，选择会被记住）。作品列表本身没有单独的「预览大小」滑块。

卡片只展示前 5 张，更多图片可在编辑状态里查看。

点开作品时先显示本地缩略图，随后临时取回原图放大显示，不写入磁盘。原图取不到时会说明原因并继续显示缩略图，不影响浏览。

放大视图右下角有「保存原图」：点一下就把这一张的原图存进该画师的「大图」文件夹，之后预览直接读本地；已经保存过的会显示「原图已保存」并置灰。它和设置里的「保存大图」开关分工不同——开关管自动保存，这个按钮管你临时决定要留下的那一张。识别画师时的候选预览还没有归属画师，所以那里不显示这个按钮。

大库建议复制完整「数据」目录备份。JSON 导出采用逐张读取写入。

## 备份包含什么

「导出备份」写出的是一个 JSON 文件（`画师库备份_年-月-日.json`），内容**只有资料与图片地址，不含任何图片数据**：

- **缩略图、原图都不内嵌**。本地 `缩略图/`、`大图/` 里的文件一律不进备份，作品里的图片字段在备份中都是空的。
- **地址原样保留**：`thumbUrl` / `previewUrl` / `largeUrl` 照抄进备份，导出时不会去 Danbooru 下载任何东西。
- 因此备份很小：一条作品只有几百字节。101 件作品的样例备份是 **20 KB**；同样这批数据在「内嵌缩略图 + 原图」的旧做法下是 **约 200 MB**。你那 657 条作品的库，备份从 16.3 MB 降到大约 **0.25 MB**。
- 恢复（导入）之后，**缩略图与原图都靠链接重新取**，所以要看图得联网、并且装着「画师库 · 图片助手」扩展（`file://` 页面直连 Danbooru 会被站点拦，这正是扩展的用处）。

**这套备份不管图片本体**，所以：

| 你想要 | 该怎么做 |
|---|---|
| 换电脑/迁移，只带资料与分类，图片能重新下载 | 「导出备份」就够（Danbooru 上还在的作品都能按链接取回） |
| **图本身也要留住**（怕站点删图、链接失效，或保留了你自己上传/生成的原图） | **复制整个「数据」文件夹**，那是逐字节完整的 |

需要特别注意：**你自己上传的图片、NovelAI 生成的测试风格图没有在线地址**，它们在本地上传/生成时就只存在「数据」文件夹里。备份不带走它们，恢复后这些格子会是空的——**这些图只有「数据」文件夹里有**，请一定把文件夹单独留好。

体积参考：JSON 导出采用逐张读取写入，体积基本就是纯文本。导入上限目前是 **100 MB**（写死在代码里）：因为导入时要把整个 JSON 一次性解析进内存，再大容易把标签页撑爆。现在备份只有几百 KB，这个上限基本不会再碰到；真要放宽得把导入改成边读边解析，不是改个数字就完事——需要的话可以单独做。


## 保存与开发回归检查

后台刷新和采集只把查询结果合并进最新资料，写文件按顺序执行。采集、刷新、生成或拖图任务尚未结束时，暂不能切换数据文件夹。改名迁移如果遇到图片读取、复制或索引写入失败，会保留原目录，排除文件占用或权限问题后可重试；只有迁移成功才清理旧目录。

开发者可运行 `npm run test:browser` 做真实浏览器回归（需要 Node.js 22 以上及 Chrome 或 Edge；也可用 `BROWSER_PATH` 指定浏览器路径）。脚本启动独立的无界面浏览器和临时配置，使用浏览器私有文件系统创建测试库，检查设置持久化、并发刷新、同路径图片替换及编辑保存。它不连接已有浏览器会话，不读写项目的「数据」目录，也不会请求 Danbooru 或 NovelAI。扩展后台协议继续由 `npm test` 的离线测试覆盖。
