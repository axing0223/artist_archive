# 依赖与日志
node_modules/
npm-debug.log*
*.log

# 画师库的个人数据：整个「数据」目录都不进版本库。
# 要留档、迁移或恢复，请复制整个「数据」文件夹（资料 JSON 与图片一起），不要依赖 Git。
数据/

# 本地数据备份（体积可达几百 MB，同样不进版本库）
备份/

# 系统临时文件
Thumbs.db
Desktop.ini
.DS_Store
*.tmp

# 构建生成的扩展内画师库镜像（源在 app/）
图片取图扩展/app/

# 本地界面验收产物
.ui-artifacts/

# 只留本机、不进版本库的开发材料。
# 这几份在 2026-09-22 发布前被从**全部历史**里清除过（当时它们已经进过版本库）；
# 文件仍留在磁盘上，只是 Git 不再跟踪。要看内容就直接打开本地文件。
# 清除它们的原因：属于个人采集清单、开发过程记录或未完成的验收细节，不适合公开。
画师表.txt
后续待办.md
发版验收-1.0.0.md
性能基线-改标签.md
性能基线-批量操作.md
UI-草案存档.md

# 没有被任何代码引用的历史图标（改用 icon16/32/48/128.png），不进版本库
图片取图扩展/icons/icon.webp
